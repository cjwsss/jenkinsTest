pipeline {
    agent any

    parameters {
      string name: 'myName', trim: true
    }

    stages {
        stage('hello') {
            steps {
                echo "${params.myNam}"
            }
        }
    }
}
