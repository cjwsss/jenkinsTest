pipeline {
    agent any

    parameters {
      string name: 'myName', trim: true
    }

    stages {
        stage('Hello') {
            steps {
                dir('./'){
                    script {
                    sh script: '''
                      #!/bin/bash
                      cd /var/jenkins_home/workspace/hcmlabDeploy
                      bash test.sh ${params.myName}
                      '''
                    }
                }
            }
        }
    }
}
