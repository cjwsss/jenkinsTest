pipeline {
    agent any

    parameters {
      string name: 'myName', trim: true
    }

    environment {
            myName = "${params.myName}"
        }

    stages {
        stage($myName) {
            steps {
                script {
                    sh ./test.sh $myName
                }
            }
        }
    }
}
