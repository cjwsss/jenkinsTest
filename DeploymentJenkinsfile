pipeline {
    agent any

    parameters {
      string name: 'USERNAME', trim: true
    }

    environment {
            username = "${params.USERNAME}"
        }

    stages {
        stage($USERNAME) {
            steps {
                script {
                    sh './test.sh $USERNAME'
                }
            }
        }
    }
}
