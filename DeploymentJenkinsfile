pipeline {
    agent any

    parameters {
      string name: 'myName', trim: true
    }

    environment {
            username = "${params.myName}"
        }

    stages {
        stage($myName) {
            steps {
                script {
                    sh './test.sh $username'
                }
            }
        }
    }
}
