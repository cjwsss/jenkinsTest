pipeline {
    agent any

    parameters {
      string name: 'myName', trim: true
    }

    stages {
        stage('hello') {
            steps {
            def myName = ${params.myName}
                script {
                    sh '''
                        #!/bin/bash
                        ./test.sh myName
                    '''
                }
            }
        }
    }
}
