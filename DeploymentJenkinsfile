pipeline {
    agent any

    parameters {
      string name: 'myName', trim: true
    }

    stages {
        stage('"${params.myName}"') {
            steps {
                sh 'echo "${params.myName}"'
            }
        }
    }
}
