pipeline {
    agent any

    parameters {
      string name: 'myName', trim: true
    }

    stages {
        stage('hello') {
            steps {
                script {
                    sh '''
                        #!/bin/bash
                        ./test.sh 'cjw'
                    '''
                }
            }
        }
    }
}
