pipeline {
    agent any

    parameters {
      string name: 'myName', trim: true
    }

    stages {
        stage('Hello') {
            steps {
                script {
                    bash './test.sh ${params.myName}'
                }
            }
        }
    }
}
